In this project, using advertising data and a logistic regression (classification) 
algorithm, I have obtained a model that predicts whether a user will click or not.
During this project, data grouping and visualization were used to find meaningful 
relationships between features and a final evaluation was used to determine the
accuracy of the model.

This model is still being developed and optimized ...